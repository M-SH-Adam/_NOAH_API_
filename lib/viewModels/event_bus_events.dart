// any dart class can be event in event bus
/*import '../../models/user.dart';

/// TODO add here any global events throw the app any private event can be
/// added in view model file or make new file for it. ///


class GetUserData{}

class SaveUserData{
  User user;
  SaveUserData(this.user);
}

class UserLoggedIn{
  User user;
  UserLoggedIn(this.user);
}
*/
